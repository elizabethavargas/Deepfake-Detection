# Deepfake Detection with DINOv2-small

This project explores using a finetuned DINOv2-small model for detecting deepfake faces. We experiment with training only a classifier head versus full model finetuning, and visualize which regions the model focuses on using attention maps.


